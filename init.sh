git submodule add https://github.com/codelab-kr/tuplus-db-fixture-rest-api.git db-fixture-rest-api
git submodule add https://github.com/codelab-kr/tuplus-history.git history
git submodule add https://github.com/codelab-kr/tuplus-gateway.git gateway
git submodule add https://github.com/codelab-kr/tuplus-metadata.git metadata
git submodule add https://github.com/codelab-kr/tuplus-mock-storage.git mock-storage
git submodule add https://github.com/codelab-kr/tuplus-oci-storage.git oci-storage
git submodule add https://github.com/codelab-kr/tuplus-security.git security
git submodule add https://github.com/codelab-kr/tuplus-terraform.git terraform
git submodule add https://github.com/codelab-kr/tuplus-video-streaming.git video-streaming
git submodule add https://github.com/codelab-kr/tuplus-video-upload.git video-upload



# db-fixture-rest-api
# gateway
# history
# metadata
# mock-storage
# oci-storage
# security
# terraform
# video-streaming
# video-upload

# echo "tuflix" && npm install
# echo "oci-storage" && cd oci-storage && npm install
# echo "gateway" && cd gateway && npm install
# echo "metadata" && cd metadata && npm install
# echo "video-streaming" && cd video-streaming && npm install
# echo "video-upload" && cd video-upload && npm install
# echo "history" && cd history && npm install
